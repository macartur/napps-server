[tox]
envlist = py36

[testenv]
whitelist_externals=
  rm

commands=
    ; Force packaging even if setup.{py,cfg} haven't changed
    rm -rf  ./napps_server.egg-info/
    pylama napps_server utils

deps=
    -rrequirements-dev.txt

